# Part 4: Customer Analytics (Chapters 4.1–4.2)
# Tested with Python 3.12. Sec4.1 uses marketing-science/environment.yml;
# sec4.2 uses marketing-science-clv/environment-clv.yml.

# --- Segmentation (4.1) ---
scikit-learn>=1.6
scipy>=1.14
bertopic>=0.16
sentence-transformers>=3.0
umap-learn>=0.5
hdbscan>=0.8.38
litellm>=1.50,<2          # LLM-assisted topic naming (sec4.1 semantic)
anthropic>=0.40           # Anthropic backend for litellm

# --- CLV (4.2) ---
pymc-marketing==1.0.0
arviz>=1.2,<2
arviz-plots[matplotlib]>=1.2
xarray>=2024.0
openpyxl>=3.1.0
kagglehub>=0.3            # Dunnhumby "The Complete Journey" fetch

# --- LightGBM companion (lightgbm-companion/, a fixed-horizon alternative to 4.2) ---
lightgbm>=4.5
shap>=0.46                # feature attribution in 03_evaluation.py
joblib>=1.3               # model persistence between the three scripts
pyarrow>=14.0             # the scripts hand features between stages as parquet

# --- shared ---
numpy>=2.0
pandas>=2.0
matplotlib>=3.8
