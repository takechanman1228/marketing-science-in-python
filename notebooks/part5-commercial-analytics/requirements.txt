# Part 5: Commercial Analytics (Chapters 5.1–5.3)
# Tested with Python 3.12, conda env: marketing-science

# --- Price elasticity (5.1) ---
scipy>=1.14
kagglehub>=0.3            # Dunnhumby "The Complete Journey" fetch

# --- Sales forecasting (5.3) ---
statsmodels>=0.14
mlforecast>=0.13.0
lightgbm>=4.5.0
prophet>=1.1.6
# TimesFM is measured in 5.3 too, but it is NOT installed here: it needs
# torch plus a foundation-model checkpoint. See requirements-timesfm.txt.

# --- shared ---
scikit-learn>=1.6
pyarrow>=14.0
numpy>=2.0
pandas>=2.0
matplotlib>=3.8
