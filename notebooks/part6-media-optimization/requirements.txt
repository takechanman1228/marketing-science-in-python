# Part 6: Marketing Mix Modeling (Chapter 6.2 Fundamentals, Chapter 6.3 Calibration)
# Local (CPU, macOS-arm): TF 2.20 hangs Meridian.__init__ — keep TF < 2.20.
# protobuf 6/7.x pollutes notebook output with MessageFactory.GetPrototype
# errors during TF / TFP import — keep protobuf < 6.
# Colab (GPU): use %pip install google-meridian[colab,and-cuda]
google-meridian[schema]
tensorflow>=2.19,<2.20
tensorflow-probability>=0.25,<0.26
protobuf>=4.21.6,<6
numpy>=2.0
pandas>=2.0
# matplotlib is used by the notebook itself (ground-truth ROI scatter) and by
# regenerate_sec6_{2,3}_figures.py. pillow is used by the regen scripts for
# .webp companions. Both are usually pulled in transitively but pin them
# explicitly so a clean clone reproduces the figures without surprises.
matplotlib>=3.8
pillow>=10
