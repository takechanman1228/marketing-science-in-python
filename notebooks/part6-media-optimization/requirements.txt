# Part 6: MMM and incrementality (Chapters 6.2–6.4)
# Local (CPU, macOS-arm): TF 2.20 hangs Meridian.__init__ — keep TF < 2.20.
# protobuf 6/7.x pollutes notebook output with MessageFactory.GetPrototype
# errors during TF / TFP import — keep protobuf < 6.
# Colab (GPU): use %pip install google-meridian[colab,and-cuda]
google-meridian[schema]==1.6.2
tensorflow>=2.19,<2.20
tensorflow-probability>=0.25,<0.26
protobuf>=4.21.6,<6
numpy>=2.0
pandas>=2.0
# matplotlib draws the notebook's own charts, including the static PNGs it
# writes so the results are visible on GitHub; pillow writes their .webp
# companions. Both are usually pulled in transitively, but pinning them keeps
# a clean clone reproducing the figures without surprises.
matplotlib>=3.8
pillow>=10
# Used by sec6.3_geo_experiment; no TensorFlow interaction.
statsmodels>=0.14
scipy
