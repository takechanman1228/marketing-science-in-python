"""msbook — companion package for 'Marketing Science in Python'.

Exposes:
    msbook.paths   — repo-root-anchored path helpers
    msbook.data    — common dataset loaders (raw / generated tiers)
"""

__version__ = "0.1.0"
